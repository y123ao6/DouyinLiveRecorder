# =============================================================================
# DouyinLiveRecorder .gitignore
# -----------------------------------------------------------------------------
# 原则：忽略字节码 / 构建产物 / 虚拟环境 / 测试与覆盖率 / 敏感配置 / 运行期产物
#   （录制文件、日志、备份）与本地工具目录；保留源码、编译好的 .mo（gettext 运行时必需）、
#   StopRecording.vbs 等分发脚本。
# 运行时资源 config/、ffmpeg/、node/ 与 exe 同级，但 config/*.ini 含凭据信息需忽略。
# =============================================================================

# Byte-compiled / optimized / DLL files
__pycache__/
*.py[cod]
*$py.class

# C extensions
*.so

# Distribution / packaging
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
share/python-wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST

# PyInstaller
# Usually these files are written by a python script from a template
# before PyInstaller builds the exe, so as to inject date/other infos into it.
*.manifest
*.spec

# Installer logs
pip-log.txt
pip-delete-this-directory.txt

# Unit test / coverage reports
htmlcov/
.tox/
.nox/
.coverage
.coverage.*
.cache
nosetests.xml
coverage.xml
coverage.json
*.cover
*.py,cover
.hypothesis/
.pytest_cache/
cover/

# 排查 / 测试期的一次性临时脚本与其输出（仅兜底；收尾仍须按 AGENTS.md「测试收尾清理临时脚本」删除）
*_tmp_*.py
tmp_*.py
*_tmp_*.txt
tmp_*.txt

# Translations
*.pot
# 保留编译好的 .mo 文件用于分发（gettext 运行时必需，Docker / 发布 zip 均依赖）
# *.mo

# Django stuff:
*.log
local_settings.py
db.sqlite3
db.sqlite3-journal

# Flask stuff:
instance/
.webassets-cache

# Scrapy stuff:
.scrapy

# Sphinx documentation
docs/_build/

# PyBuilder
.pybuilder/
target/

# Jupyter Notebook
.ipynb_checkpoints

# IPython
profile_default/
ipython_config.py

# ---------------- DouyinLiveRecorder 专用（运行时自动生成目录）----------------
# 注意（MID-2255，与 .dockerignore 同族修正）：本文件里 `#` **只有行首才是注释**，行内 `#` 会被
# 当成模式字面量——原先四条带行内说明的写法各自变成一条永不匹配的长模式（`git check-ignore
# node/foo` 不命中即为证），其中 node/ 与 node-v*.zip 再无其它兜底规则。忽略意图不变，只把注释移到独立行。
# 运行时配置自动备份（main.py 修改 config 时生成）
backup_config/
# 运行日志（logs/streamget.log 等，DEBUG/ERROR 落盘）
logs/
# 运行时自动下载的 Node.js（Windows 专用，容器内改用 apt 安装）
node/
# Node.js 安装包
node-v*.zip

# pyenv
# For a library or package, you might want to ignore these files since the code is
# intended to run in multiple environments; otherwise, check them in:
.python-version

# pipenv
# According to pypa/pipenv#598, it is recommended to include Pipfile.lock in version control.
# However, in case of collaboration, if having platform-specific dependencies or dependencies
# having no cross-platform support, pipenv may install dependencies that don't work, or not
# install all needed dependencies.
#Pipfile.lock

# poetry
# Similar to Pipfile.lock, it is generally recommended to include poetry.lock in version control.
# This is especially recommended for binary packages to ensure reproducibility, and is more
# commonly ignored for libraries.
# https://python-poetry.org/docs/basic-usage/#commit-your-poetrylock-file-to-version-control
poetry.lock

# pdm
# Similar to Pipfile.lock, it is generally recommended to include pdm.lock in version control.
#pdm.lock
# pdm stores project-wide configurations in .pdm.toml, but it is recommended to not include it
# in version control.
# https://pdm.fming.dev/#use-with-ide
.pdm.toml

# PEP 582; used by e.g. github.com/David-OConnor/pyflow and github.com/pdm-project/pdm
__pypackages__/

# Celery stuff
celerybeat-schedule
celerybeat.pid

# SageMath parsed files
*.sage.py

# Environments
.env
.venv
env/
venv/
ENV/
env.bak/
venv.bak/

# Spyder project settings
.spyderproject
.spyproject

# Rope project settings
.ropeproject

# mkdocs documentation
/site

# mypy
.mypy_cache/
.dmypy.json
dmypy.json

# Pyre type checker
.pyre/

# pytype static type analyzer
.pytype/

# Cython debug symbols
cython_debug/

# PyCharm
# JetBrains specific template is maintained in a separate JetBrains.gitignore that can
# be found at https://github.com/github/gitignore/blob/main/Global/JetBrains.gitignore
# and can be added to the global gitignore or merged into this file. For a more nuclear
# option (not recommended) you can uncomment the following to ignore the entire idea folder.
.idea/
*.iml
*.iws
*.ipr

# VS Code
.vscode/
*.code-workspace

# Sublime Text
*.sublime-project
*.sublime-workspace

# Vim
*.swp
*.swo
*~

# ---------------- DouyinLiveRecorder 项目专用配置 ----------------
# 配置文件（含敏感信息：录制账号 Cookie / 代理 / Web 密码等，禁止入库）
config/config.ini
config/URL_config.ini

# FFmpeg 二进制文件（Windows 分发用；容器内改用 apt 安装，故忽略宿主机二进制）
ffmpeg/*.exe
ffmpeg/*.dll
ffmpeg/ffmpeg
ffmpeg/ffprobe

# 录制的音视频与弹幕产物（视频 .ts/.mp4/.flv 与弹幕 .srt 均落盘于此，禁止入库）
downloads/
recordings/

backup_config/

# 日志文件（含 logs/ 目录与根目录散落日志）
logs/
*.log
*.log.*
# 弹幕监控边车日志（logs/danmaku_monitor.jsonl；与 .dockerignore 同源维护）
*.jsonl

# .mo 编译翻译文件随仓库分发（gettext 运行时必需，不忽略）
# 注：下面一条是空规则——compile_po.py 真身在 scripts/ 下，i18n/ 内从来没有该文件
# （与 .dockerignore 侧 MID-2255 已删除的同名规则同源核对）
i18n/**/compile_po.py

# Windows 特定文件
Thumbs.db
desktop.ini
.DS_Store
*.exe
*.dll
*.cmd
*.bat
*.vbs
# StopRecording.vbs 是项目自带的停止录制脚本，需要随仓库分发（否定上面的 *.vbs）
!StopRecording.vbs

# macOS 特定文件
.DS_Store
.AppleDouble
.LSOverride

# Linux 特定文件
*~

# 临时文件
*.tmp
*.temp
*.bak
*.swp
*.swo

# 过程性探针 / 调试残留（非源码，如 src/stream.pyc_probe_tmp，勿提交）
*.pyc_probe_tmp

# 系统托盘图标缓存
*.icon

# AI 助手工作目录（含本项目记忆 / 技能 / 第三方代理生成物，禁止入库）
.workbuddy/
.codebuddy/
.trae/

# 临时 / 过程性文档（只兜底开发期随手记的临时笔记名；CODE_WIKI*.md / CODE_REVIEW_FIX_1.md /
# DIAGNOSIS_*.md 是正式架构与审查记录，随仓库分发、不忽略）
# PROPOSAL_*.md（待审批的生产代码改动提案）同属正式记录，刻意**不在此处忽略**，只在
# .dockerignore 里排除以免增大镜像；新增同类前缀时两处同改。
CODE_CHANGES.md
TRAE_AGENT_CODE_WIKI.md
PERF_REVIEW_*.md

# 本地工具 / 审查生成物（与 .dockerignore 及 pyproject 各工具排除列表同源维护）
.qoder/
.qoder-credits/
.agents/
.pnpm-store/
.npm-cache/
.dsh-validation/
.ego-browser-test/
.plugin-src/
.tmp-dps-extract/
.mimosa/
.v2c/

# 测试输出目录（端到端 / 真实房间采集的临时产物）
tests/_out_e2e/
tests/_out_live/

# pytest 缓存残留（conftest 落盘型 cache 目录，曾被 basedpyright 误扫描）
pytest-cache-files-*/

# isort 备份残留（格式化工具偶发生成的 .py.isorted，需清理避免误提交）
*.isorted

# 审查修复轮的临时产物（2026-09-22/23 新增形态；与 .dockerignore 同源维护）
# _out_*.txt 为各组修复代理按简报约定的重定向输出（AGENTS.md「临时输出文件」命名），收尾须自行
# 删除、此处仅兜底防误提交；_probe_*.py 为并行修复组新建的临时探针脚本，归属主会话裁定后再定去留。
# 刻意**不**忽略 _FIX_BRIEF_*.md / _i18n_pending.json / _run.py —— 那三个由主会话在收尾统一处置，
# 忽略会让「忘记删」变成永久静默。
_out_*.txt
_probe_*.py
