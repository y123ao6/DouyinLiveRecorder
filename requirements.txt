# DouyinLiveRecorder Python 依赖
# 最低版本要求，安装: pip install -r requirements.txt

# HTTP 请求
requests>=2.28.0
httpx[http2]>=0.25.0

# 日志
loguru>=0.7.0

# 加密（SM3、RC4 等签名算法）
pycryptodome>=3.15.0

# Linux 发行版检测
distro>=1.8.0

# 终端进度条
tqdm>=4.65.0

# JavaScript 执行引擎（Node.js 驱动，用于平台签名脚本）
PyExecJS>=1.5.1

# GUI 系统托盘（仅桌面环境需要）
pystray>=0.19.4

# GUI 图像处理（PIL，仅桌面环境需要）
Pillow>=10.0.0

# Wevers 直播平台 SDK
weverse>=0.9.0