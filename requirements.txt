# =============================================================================
# DouyinLiveRecorder Python 运行时依赖清单
# -----------------------------------------------------------------------------
# 版本约束与 pyproject.toml [project.dependencies] 严格保持一致（单一事实源）。
# 本文件同时被以下场景复用：
#   - Docker 多阶段构建（builder 阶段 `pip install -r requirements.txt`）
#   - CI 流水线（typecheck / test / concurrency-test / integration-verify /
#     build-verify 各 job 安装依赖；static job 只装固定版本 lint 工具，不经本文件）
#   - 本地开发：`pip install -r requirements.txt`
# 构建期「可选」依赖（PyInstaller）不在此处，见 pyproject.toml
#   [project.optional-dependencies].build，通过 `pip install .[build]` 安装。
# 开发期依赖（pytest / black / isort / mypy）同理在 [project.optional-dependencies].dev，
#   通过 `pip install .[dev]` 安装；GUI 专属依赖在 .gui（本清单已含，pip 安装时一并拉取）。
# 前端用例（tests/frontend/*.mjs）用 Node 内置 node:test，零 npm 依赖，无需新增 Python 包。
# =============================================================================

# ---------- HTTP 请求 ----------
requests>=2.34.2          # 同步 HTTP 请求（现仅 ffmpeg / node 安装下载脚本使用；各平台解析走 httpx 异步面，不经本包）
# urllib3 是 requests 的传递依赖，但本仓运行期的同步出站下载（ffmpeg / node 的拉取与哈希核对：
# src/ffmpeg_install.py、src/node_install.py、src/ffmpeg_master_download.py 经 requests）都穿过它，
# 故显式声明下限而非放任解析回退（补-05）：
# CVE-2026-44431（跨域重定向时敏感头被转发）受影响区间为 1.23 – < 2.7.0，本机实测已装 2.8.0。
# 下限只作「不得解析回退到受影响段」的保护，不设上限（与全库「只有 protobuf 有上限」一致）。
# [历史注] 2026-09-24 更正：旧表述「全部同步出站 = src/sync_http.py 约 125 调用点 → src/spider.py
#   各平台解析」已被推翻——spider 走 httpx 异步面（src/async_http.py），sync_http.py 现无生产 importer。
urllib3>=2.7.0            # 传输层（requests 之下；显式声明以防重装/解析回退落入 CVE-2026-44431 区间）
httpx[http2]>=0.28.1      # 异步 HTTP 客户端（含 HTTP/2，src/async_http.py 并发抓取流地址）
# ── httpx 的**可选**能力包（extra），本仓运行时真的会执行其代码，故必须显式声明 ──
# 为什么不靠 httpx[http2] 的 extra 传递：本清单是 Docker/CI `pip install -r` 的实际输入，
#   而 extra 只在「pip 解析 .[extra] 这一条」时展开；把 httpx 写成裸名或换索引重装即静默丢失。
#   2026-09-23 本机实测：`import h2` → ModuleNotFoundError（h2-4.4.1.dist-info 为空壳目录、
#   METADATA 已失，属 pip uninstall 残留），而复现路径完全落在正常用法内。
# 触发点（已回源到 httpx 源码，非推测）：
#   · h2        —— httpx._client.Client.__init__ 中 `if http2: import h2`；
#                  本仓 http2 默认即 True（src/async_http.py 的 async_req 形参默认值），
#                  且 httpx.AsyncClient 未显式传 http2 时按 httpx 自身默认 True 构造。
#   · socksio   —— httpx._transports.default 在 `proxy.url.scheme in ("socks5","socks5h")` 分支
#                  `import socksio`；本仓明确支持并校验 socks5:// 写法，用户配 SOCKS 代理即命中。
# 二者都是**运行期** ImportError，而清单里查不到 —— 正是「pip install 完仍缺包」的成因。
# 下限取「本机可解析且不低于 2026-09-23 安全基线」的值，不设上限（与全库口径一致，protobuf 例外）。
# 维护锁：tests/test_regression_2026_09_22_gates.py 断言本文件与 pyproject [project.dependencies]
#   的包名集合逐项一致——单侧遗漏（正是本次的形态）会被该用例直接抓红。
h2>=4.3.0                 # httpx http2=True 的运行期依赖（httpx Client.__init__ 内 `import h2`）
socksio>=1.0.0            # httpx SOCKS 代理（socks5/socks5h）的运行期依赖（httpx 传输层内 `import socksio`）

# ---------- 日志 ----------
loguru>=0.7.3             # 结构化日志（src/logger.py 统一封装，DEBUG/ERROR 落盘 logs/streamget.log）

# ---------- 加密 / 签名 ----------
pycryptodome>=3.23.0      # 加密算法（SM3 / RC4 / AES，部分平台签名与响应解密）

# ---------- 系统检测 ----------
distro>=1.9.0             # Linux 发行版识别（自动选择下载源 / 给出安装提示）

# ---------- CLI 进度条 ----------
tqdm>=4.69.0              # 下载进度条显示（命令行录制模式）

# ---------- JavaScript 执行引擎（运行各平台 JS 签名脚本，需 Node.js 运行时）----------
exejs>=1.0.1              # PyExecJS 的活跃维护继任者，优先使用（room/spider/utils 导入）
PyExecJS>=1.5.1           # 回退兼容：exejs 未安装时启用（已停止维护，仅作兜底）

# ---------- GUI 图形界面（headless / Web 模式下非必需，但 pip 安装仍会拉取）----------
customtkinter>=6.0.0      # 现代化 GUI 框架（圆角控件、深浅色主题、DPI 自适应）
pystray>=0.19.5           # 系统托盘图标（GUI / Web 托盘模式；0.19.5 起 macOS 改用 PNG 替代 TIFF）
Pillow>=12.3.0            # 图像处理（托盘图标生成 / 缩略图）

# 注：不列入 pip 上的 weverse 包——该包会拉入已废弃的 pycrypto==2.6.1，在 Python 3.10+ 上因缺失
# longintrepr.h 头文件无法编译。[历史注] 原 src/weverse_auth.py（经 requests 直调 API 的 Weverse
# 认证实现）已于 2026-09-23 整体删除（删除前已无生产调用点），此处仅保留「勿加 weverse 包」的理由备查。

# ---------- Web 管理面板（FastAPI + uvicorn）----------
fastapi>=0.140.0           # Web 框架（src/web_api.py，录制引擎守护线程之上的 HTTP 接口）
# starlette 下限由 0.49.1 抬到 1.0.1（补-05）：CVE-2026-48710（BadHost：Host 头未校验 →
# 认证/缓存投毒类）的受影响区间是 **<= 1.0.0**，旧下限把 0.49.1–1.0.0 整段留在合法安装区间内，
# 换索引/重装即可能解析回退到有漏洞的版本。本机与 CI 实测已装 1.6.0，抬下限不改变现装集合。
# [2026-09-21 pip-audit 实测] 上面 1.0.1 → 1.3.1 的二次抬升理由：补-05 抬到的 1.0.1 **本身就是受影响版本**
# （PYSEC-2026-2280 / PYSEC-2026-2281 修复于 1.1.0；PYSEC-2026-248 / PYSEC-2026-249 修复于 1.3.0 / 1.3.1），
# 故 >=1.0.1 仍放行 1.0.1–1.3.0 整段。现装 1.6.0，抬下限不改现集合、无需重装。
# 教训：`pip-audit -r requirements.txt`（解析模式）只会解析到**区间内最新版本**，
# 对「下限自身落在受影响段」完全失明——同一条清单解析模式 rc=0、下限模式 rc=1。
# 因此 ci.yml 的 deps-audit job 现另有一条「下限复核」步骤（--no-deps 审计逐项下限）。
starlette>=1.3.1          # ASGI 工具集（fastapi 传递依赖，src/web_api.py 直接导入故显式声明）
uvicorn[standard]>=0.51.0  # ASGI 服务器（web.py 启动面板，[standard] 含 uvloop/ws 等可选件）
python-multipart>=0.0.32   # 表单 / 文件上传解析（Web 接口接收配置上传）
pydantic>=2.13.4           # 请求模型校验（FastAPI 依赖，代码直接引用 BaseModel）

# ---------- 弹幕（直播弹幕采集）运行时依赖 ----------
# 以下三个包均为「运行时必需」依赖，由弹幕采集链使用（src/collector.py 工厂 +
# src/platforms/ 各平台采集器 + src/proto/ 协议解码），并非构建期依赖。
# 缺失会导致：venv 安装后 web.py 启动报 ModuleNotFoundError（douyin_pb2 导入链），
# 且弹幕采集分支因无法导入而整体失效（2026-08 工作日志已确认需补装）。
websockets>=14.0          # 弹幕 WebSocket 客户端（src/ws_client.py；additional_headers/proxy 参数为 14.0+ API，
                         # 装到 12.x/13.x 时 connect() 直接 TypeError 且被重连循环吞掉，表现为弹幕永远连不上）
# [2026-09-21 pip-audit 实测] 下界由 6.31.1 抬到 6.33.5：PYSEC-2026-1805 影响 6.31.1 本身，
# 官方修复版本为 5.29.6 / 6.33.5 —— 旧下界正好落在受影响段内。上限 <8 保持不动（F-14）。
# 现装 7.36.1，抬下界不改变现装集合；tests/test_proto_runtime_compat.py 动态读取本区间、随之通过。
protobuf>=6.33.5,<8       # 抖音弹幕协议解码（src/proto/douyin_pb2.py，protoc 生成模块）。上限 <8：该生成文件由 protoc 25.x（gencode 4.25.3）产出，跨大版本升级 runtime 存在 gencode 兼容窗口外突然失效的风险（FIX_1 F-14）；升级 runtime 前须先用同代 protoc 重新生成并回归弹幕用例
brotli>=1.2.0             # B站弹幕解压（src/platforms/bilibili.py，protover=3 需 brotli 解压）

# ---------- 国际化（i18n）----------
PyYAML>=6.0.3             # YAML 翻译目录支持（i18n.py 加载 i18n/zh_TW.yaml；缺失仅损失 YAML 格式）

# 注：可执行文件打包需 PyInstaller，仅构建期使用、非运行时依赖：
#   pip install .[build]   或   pip install pyinstaller>=6.10.0
# （PyInstaller 不在本清单内，由 pyproject.toml [project.optional-dependencies].build 声明）
